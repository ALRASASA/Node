// modules
//Common JS every file is module by default
const secret='superman'
const reqd = 'reqd'
const sayhi=(name) =>{
    console.log(`hello there ${name}`);
}

sayhi('jasin')
sayhi(reqd)
sayhi(secret)